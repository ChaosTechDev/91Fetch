# 91Fetch v1.1.1

这是 91Fetch 的 Windows 本地桌面版维护更新。软件用于浏览、采集和批量下载 91porn 网站的 viewkey 视频，运行后通过本机网页界面操作。

## 本次更新

- 完善中文和英文安装、运行及数据保存说明
- 新增版本更新记录
- 明确升级时需要保留 `downloads/` 数据目录
- 整理 Windows 发布压缩包，只包含本地运行需要的文件

## 使用方法

1. 下载 `91Fetch-v1.1.1-Windows.zip` 并解压。
2. 双击 `启动.cmd`。
3. 首次运行会自动创建 Python 环境、安装依赖并打开网页。
4. 默认访问地址为 `http://127.0.0.1:8765`。

需要 Windows 10/11 和 Python 3.11 或更高版本。

## 升级说明

从旧版本升级时，请保留原来的 `downloads/` 目录。该目录包含已下载视频、下载任务状态、设置和本地识别记录。

项目采用 MIT License。使用者应遵守目标站点条款及所在地规定。
