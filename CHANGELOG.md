# 更新记录

## 1.1.6 (Bug Fix Release)

- 🔒 **Critical**: 修复登录时 Cookie 保存顺序错误（session.close() 调整到 save_session_cookies() 之后）
- ✨ **Compatibility**: 扩大端口探测范围从 20 到 100，提升 Windows 环境下启动成功率
- 📝 **Documentation**: 新增 BUG_AUDIT_REPORT.md 和 FINAL_SUMMARY.md，完整的技术审计报告
- 🔧 **Network**: 修复 yt-dlp 盗版网站检测导致的下载失败问题，现在可以正常下载视频

> **建议**: 强烈建议所有用户升级到 v1.1.6，特别是遇到登录失效或下载 Piracy 错误的用户

## 1.1.5


- 新增「账号登录」：设置中心输入账号、密码和验证码即可直接登录站点，登录态 Cookie 自动保存，采集与下载请求全部携带
- 支持检测登录状态、退出登录；也保留粘贴浏览器导出 Cookie 的备选方式
- 新增「优先获取高清视频」选项：解析视频时优先访问高清版页面（view_video_hd.php），需登录（VIP）账号才有高清流，无权限时自动回退普通画质
- 实测确认：站点高清画质仅对登录会话开放，匿名高清页不含视频源，此时自动回退普通画质
- 命令行 crawl/download/run 新增 `--hd` 参数，配合 `--cookies` 使用
- 设置页重新分区：账号登录 → 存储与下载 → 界面刷新
- 网页端改版为 NAS 风格侧栏：左侧分类区跟随视图联动，视频目录显示视频分类、下载管理显示任务分类、设置中心显示设置分类
- 任务分类精简为「未完成 / 已下载 / 下载失败」三项，默认显示未完成；正在下载、等待等明细数量保留在顶部汇总
- 修复切换任务分类时滚动条闪现抖动的问题（窗口滚动条常驻显示，切换后回到列表顶部）
- 设置中心按左侧分类真正分区展示（账号登录 / 存储与下载 / 界面刷新），点哪项只显示哪区，不再全部堆在一页
- 修复复选框旁多渲染一个方框的问题（文字 span 被误套了勾选框样式）
- 修复登录验证码获取失败的问题：验证码请求复用采集的限速重试客户端，站点瞬断连接时自动重试，登录提交同样获得重试保护
- 下载管理全选按钮只作用于当前筛选结果，切换筛选后自动取消勾选

## 1.1.4

- 修复下载前刷新视频地址时始终从列表第 1 页获取新链接的问题，第 2 页以后的视频不再误报"列表会话已过期"
- 修复 Netscape Cookie 导入会丢失所有 HttpOnly Cookie 的问题（会话 Cookie 现在可以正常导入）
- 采集任务进行中时，界面改为读取本地库展示进度，不再高频请求目标站触发人机验证
- 修复人机验证页检测在部分 Cloudflare 站点上误报的问题，恢复仅对 403/503 做挑战检测
- 文件名处理兼容 Windows：截断过长标题、避开 CON/NUL 等保留设备名
- 小体积合法视频不再被误判为 CDN 空响应（阈值从 256KB 降至 64KB）
- 修复 Web 端作者采集未对 UID 做 URL 转义的问题
- 修复多个并发一致性问题：下载入队竞态、锁外修改共享数据、任务删除后状态卡死
- 清单/目录文件中的坏数据行现在会被跳过并记录日志，不再导致整体崩溃
- 下载目录扫描增加 5 秒缓存，界面轮询不再每次全盘扫描校验文件
- 移除从未使用的 `next_page_selector` 配置项（读取时自动忽略，兼容旧 site.json）
- 启动器兼容 Linux/macOS 的 venv 路径

## 1.1.3

- 修复下载目录扫描遇到临时文件、权限变化或文件被移动时可能返回 HTTP 500
- 下载管理轮询遇到异常时改为限频提示，避免错误通知连续刷屏
- 非 JSON 的服务器错误现在会显示明确的 HTTP 状态和返回内容
- 本地库存短暂不一致时，下一次轮询可以自动恢复，不需要反复刷新页面

## 1.1.2

- 修复项目解压到中文路径后双击启动失败的问题
- 将 Windows 启动流程移到 `launcher.py`，使用 `Path.resolve()` 正确定位项目目录
- 启动器统一处理虚拟环境创建、依赖安装、`site.json` 初始化和本地服务启动
- 将 `启动.cmd` 改为纯 ASCII 启动入口，降低 Windows 命令解释器编码差异造成的失败
- 清理 Windows 发布包中的缓存文件和内部工作目录
- 在中英文说明中明确 Windows 桌面版与独立 Docker 版的功能和部署区别

## 1.1.1

- 完善公开桌面版的中英文安装、运行和数据保存说明
- 新增独立更新记录，便于查看各版本变化
- 明确升级时应保留 `downloads/`，避免任务状态和本地下载索引丢失
- 发布包仅包含 Windows 本地运行所需文件

## 1.1.0

91Fetch 公开桌面版当前版本，面向 Windows 本地运行。

- 增加 91porn 分类分页浏览、每日排行、作者主页和自定义列表入口
- 展示标题、封面、作者、时长、播放量和 viewkey
- 支持选择当前页视频并加入下载队列，加入后可以继续浏览和选择
- 支持 `.m3u8` / `.mp4` 视频地址解析、HLS 分片并发、失败重试和断点续传
- 下载管理支持全部、未完成、已完成筛选，以及选择、重试和删除
- 本地扫描按 viewkey 识别已下载视频，切换分类后仍保持已下载状态
- 设置中心支持下载目录、归档方式、并发数和界面刷新间隔
- 提供日间/夜间主题、过渡动画和局部进度更新

## 运行说明

公开版本以本地网页应用方式运行，数据和视频默认保存在项目目录下的 `downloads/`。首次启动请双击 `启动.cmd`，或按 [README.md](README.md) 中的手动启动命令运行。
