# 更新记录

## 1.1.3

- 修复下载目录扫描遇到临时文件、权限变化或文件被移动时可能返回 HTTP 500
- 下载管理轮询遇到异常时改为限频提示，避免错误通知连续刷屏
- 非 JSON 的服务器错误现在会显示明确的 HTTP 状态和返回内容
- 本地库存短暂不一致时，下一次轮询可以自动恢复，不需要反复刷新页面

## 1.1.2

- 修复项目解压到中文路径后双击启动失败的问题
- 将 Windows 启动流程移到 `launcher.py`，使用 `Path.resolve()` 正确定位项目目录
- 启动器统一处理虚拟环境创建、依赖安装、`site.json` 初始化和本地服务启动
- 将 `启动.cmd` 改为纯 ASCII 启动入口，降低 Windows 命令解释器编码差异造成的失败
- 清理 Windows 发布包中的缓存文件和内部工作目录
- 在中英文说明中明确 Windows 桌面版与独立 Docker 版的功能和部署区别

## 1.1.1

- 完善公开桌面版的中英文安装、运行和数据保存说明
- 新增独立更新记录，便于查看各版本变化
- 明确升级时应保留 `downloads/`，避免任务状态和本地下载索引丢失
- 发布包仅包含 Windows 本地运行所需文件

## 1.1.0

91Fetch 公开桌面版当前版本，面向 Windows 本地运行。

- 增加 91porn 分类分页浏览、每日排行、作者主页和自定义列表入口
- 展示标题、封面、作者、时长、播放量和 viewkey
- 支持选择当前页视频并加入下载队列，加入后可以继续浏览和选择
- 支持 `.m3u8` / `.mp4` 视频地址解析、HLS 分片并发、失败重试和断点续传
- 下载管理支持全部、未完成、已完成筛选，以及选择、重试和删除
- 本地扫描按 viewkey 识别已下载视频，切换分类后仍保持已下载状态
- 设置中心支持下载目录、归档方式、并发数和界面刷新间隔
- 提供日间/夜间主题、过渡动画和局部进度更新

## 运行说明

公开版本以本地网页应用方式运行，数据和视频默认保存在项目目录下的 `downloads/`。首次启动请双击 `启动.cmd`，或按 [README.md](README.md) 中的手动启动命令运行。
