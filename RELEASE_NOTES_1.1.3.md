# 91Fetch v1.1.3

## 本次具体修改

- 修复下载目录扫描遇到临时文件、权限变化或文件被移动时可能返回 HTTP 500
- 下载管理轮询遇到异常时改为 8 秒内相同错误只提示一次，避免错误通知连续跳出
- 非 JSON 的服务器错误现在会显示 HTTP 状态和返回内容
- 本地库存短暂不一致时，下一次轮询可以自动恢复

本版本是在 v1.1.2 中文路径启动修复基础上的桌面版维护更新。Docker/NAS 版本不包含在本次 Release 中。
